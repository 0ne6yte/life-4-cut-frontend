import ReactDOM from 'react-dom/client';

import './domain/_common/styles/global.css';
import App from '@/App';

const root = ReactDOM.createRoot(document.getElementById('root') as HTMLElement);
root.render(<App />);
